import React from 'react';
import './index.less';
import { Button, TabBar } from 'antd-mobile';

export default () => {
  return (
    <div className="home-container">
      <Button>Start</Button>
    </div>
  );
};
